# 统计与广告接入边界

当前站点不加载统计或广告脚本。接入第三方服务前，先更新隐私政策并完成适用的同意和数据处理评估。

## 不可破坏的隐私边界

第三方 `<script>` 即使来自其他域名，也会在当前页面上下文中执行。工具页中的文件名、图片预览、Canvas、Blob URL、水印文字和 EXIF 表格都可能被同页脚本读取。

因此：

- 不在工具工作区页面直接加载广告联盟脚本；
- 不允许标签管理器任意注入新脚本；
- 不把文件对象、Canvas 或输出 Blob 暴露给统计回调；
- Content Security Policy 只允许本站脚本；
- 在 CDN 或托管层发送 `Content-Security-Policy: frame-ancestors 'none'`，避免工具页被第三方 iframe 嵌入；
- 如必须展示第三方广告，使用跨域、最小权限的沙箱 iframe；
- 不把同源内容同时授予 `allow-scripts` 和 `allow-same-origin` 后当作隔离方案。

## 可记录的匿名事件

仅在用户同意和隐私政策允许时记录：

- `tool_open`：工具名称；
- `tool_process_success`：工具名称、输出格式和粗粒度大小区间；
- `tool_process_error`：工具名称和预定义错误类别；
- `tool_download`：工具名称。

禁止发送图片内容、原文件名、水印文字、EXIF 值、GPS、精确输入尺寸、Blob URL 和自由文本错误信息。

## 页面位置

若后续合规接入广告，仅考虑：

1. 首页全部工具之后；
2. 工具说明文章之后；
3. 结果区完整结束后的独立区域。

禁止放在上传区、参数控件、处理按钮、下载按钮、错误提示或移动端遮挡层附近。广告必须明确标记，并预设尺寸避免布局跳动。

## 上线门槛

- 明确服务提供方、域名、脚本清单和数据流；
- 更新 CSP、隐私政策和退出方式；
- 实测生产响应头；不要使用无效的 `<meta http-equiv="X-Frame-Options">`；
- 验证无文件内容或敏感字段网络请求；
- 在移动端检查误触和布局跳动；
- 记录撤回、停用和供应商故障时的处理流程。
