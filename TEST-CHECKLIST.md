# 上线检查清单

## 自动检查

- [x] JavaScript 语法
- [x] 内部链接和静态资源路径
- [x] JSON-LD 可解析
- [x] sitemap 使用绝对 URL 和准确 `lastmod`
- [x] 公开页面不含草案、默认假设和旧品牌占位文案
- [x] 运行时代码不使用网络发送 API、`innerHTML` 或 `eval`
- [x] 文件签名识别，不依赖扩展名和 MIME 声明
- [x] 解码前拒绝超限单边和伪装文件
- [x] EXIF 字段不能读取到 APP1 段之外
- [x] 生成页面与当前配置哈希一致
- [x] 发布目录使用逐文件白名单并完成二次链接检查
- [x] JPEG 填充字节、超长头部和晚出现的 APNG 动画块
- [x] 错误 MIME 修正、零宽水印拒绝和新文件状态清理

运行：

```bash
npm run check
```

## 生产配置

- [ ] 域名能够访问，canonical 与实际 URL 完全一致
- [ ] 运营主体、联系入口和代码仓库真实可用
- [ ] `productionReady` 已使用不带引号的布尔值 `true`
- [ ] 生产构建成功，Pages 只发布 `_site/`
- [ ] 自定义域名已验证，TXT 记录保留，HTTPS 已强制
- [ ] 没有通配符 DNS 或悬空 CNAME

## 桌面与移动浏览器

- [ ] Chrome、Edge、Firefox、Safari 最新稳定版
- [ ] iOS Safari 真机
- [ ] Android Chrome 真机
- [ ] 微信内置浏览器
- [ ] QQ 内置浏览器或 QQ 浏览器
- [ ] UC、华为浏览器中的选择文件和下载
- [ ] 360px、390px、768px、1440px 无横向溢出和遮挡
- [ ] 键盘可使用菜单、上传、参数、处理、打开和下载
- [ ] 关闭 JavaScript 后正文和导航仍可阅读
- [ ] 减少动态效果设置下无强制动画

## 网络与下载

- [ ] 家庭网络、移动网络和受限网络环境至少各一次实际访问
- [ ] Pages、CSS、JS、图片和证书均成功加载
- [ ] 响应头包含 `frame-ancestors 'none'` 或 `X-Frame-Options: DENY`；否则确认 iframe 运行时保护生效
- [ ] 下载按钮、打开结果和长按保存至少有一种可用路径
- [ ] 多次处理后预览仍是最新文件，没有旧结果覆盖
- [ ] 前进、后退、刷新后不会显示已失效的 Blob 预览

## 图片样本

- [ ] 普通 JPG、透明 PNG、透明 WebP
- [ ] 文件 MIME 为空但签名正确
- [ ] 扩展名伪装、空文件、损坏文件
- [ ] 动画 PNG、动画 WebP 明确拒绝
- [ ] 接近 30 MB、8192 单边、2400 万像素边界
- [ ] 超长全景图和极端宽高比明确拒绝
- [ ] 带方向、GPS、相机信息的 JPEG
- [ ] 损坏、循环和越界 EXIF 不会卡死页面

## 五个工具

- [ ] 压缩达到目标，或明确说明未达到
- [ ] 原图已满足目标时不重新编码
- [ ] 透明 PNG 压缩到 WebP 保留透明，输出 JPG 使用白底
- [ ] 水印长文本换行，超出三行时阻止导出
- [ ] 纯零宽字符和过小画布不会生成“成功”结果
- [ ] 水印滑块显示实际数值，预览与输出一致
- [ ] 定位裁剪可调整水平和垂直焦点
- [ ] 无效尺寸不会导出旧画布
- [ ] PNG/WebP 透明留边与 JPG 背景正确
- [ ] 相同格式转换不会重复编码
- [ ] 浏览器不支持输出 MIME 时明确失败
- [ ] GPS 默认隐藏，主动点击后才显示
- [ ] JPG 清除结果重新检测不到 EXIF APP1
- [ ] 每个工具的重置、打开结果和下载正常

## 内容与搜索

- [ ] 标题、描述、正文与实际功能一致
- [ ] 关于、隐私、条款不含内部操作说明
- [ ] 工具页限制、格式变化和隐私边界准确
- [ ] Search Console、Bing Webmaster Tools 和目标中文搜索平台已按需验证
- [ ] 没有复制其他站点说明，没有批量换关键词的重复页面
- [ ] 重要内容更新时同步修改对应的 `pageLastModified`
