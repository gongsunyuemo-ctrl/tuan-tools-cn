# 更新记录

## 1.1.2 - 2026-08-25

- 将托管、隐私和兼容性说明改为不针对特定地区的中性表述。

## 1.1.1 - 2026-08-25

- 切换正式站点、代码仓库和反馈入口到 `tuan-image-tools` 组织；
- 开启生产索引配置并完成发布目录复检。

## 1.1.0 - 2026-08-25

- 完整扫描受限大小内的图片结构，修复 JPEG 填充、超长头部和晚出现动画块的识别；
- 修复压缩复用原文件时的 MIME、损坏 EXIF 状态和新文件失败后残留旧结果；
- 拒绝零宽或超出安全区域的水印，并统一预览与原尺寸导出的文字比例；
- 尺寸下载名改用实际整数尺寸，结果焦点恢复可见轮廓；
- 配置改为严格类型、HTTPS、日期和自定义域名校验，并加入逐页更新时间；
- 增加资源内容版本、生成文件哈希、逐文件发布白名单和发布目录二次检查；
- GitHub Actions 增加 PR 检查、固定 Action SHA，并拆分低权限构建与部署权限；
- 补齐社交图片尺寸与替代文本，移除会破坏 HTTP 本地预览的 CSP 指令；
- 增加 iframe 运行时保护，并记录生产响应头要求；
- 更新图标、公开测试说明和真实设备检查边界。

## 1.0.0 - 2026-08-24

- 更换为图安工具品牌和统一页面生成器；
- 修复文件签名、预解码尺寸、动画和极端宽高比检查；
- 修复透明 WebP、JPG 透明合成和输出 MIME 回退；
- 修复异步旧任务覆盖和 Blob URL 泄漏；
- 原图满足目标时不重复编码；
- 水印预览改为小画布，支持三行自动换行和数值显示；
- 尺寸工具增加定位裁剪、透明留边和独立导出画布；
- EXIF 解析限定 APP1 边界，限制字段数量并默认隐藏 GPS；
- 增加输出预览、打开结果、重置、结果聚焦和无 JavaScript 内容回退；
- 增加 CSP、完整 Open Graph、面包屑、方法页、站点地图和生产配置检查；
- GitHub Actions 只发布白名单文件，补充域名接管、站群和托管边界说明。
